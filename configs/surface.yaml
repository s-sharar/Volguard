# Layer 2 surface-construction config.
# Snap just after 08:00 UTC settlement (plan section 7).
snap_hour_utc: 8
snap_minute_utc: 5

# Observation filters.
min_tau_days: 2.0
delta_min: 0.02
delta_max: 0.98
iv_min: 0.01
iv_max: 5.0

# Standard output grids (plan section 7).
tenor_grid_days: [7, 14, 30, 60, 90, 180]
moneyness_grid: [-2, -1.5, -1, -0.5, 0, 0.5, 1, 1.5, 2]
